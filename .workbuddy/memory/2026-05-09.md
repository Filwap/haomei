## 主题色恢复为玫瑰粉色系
- 用户决定不使用淡蓝色，恢复原来的玫瑰粉/玫红色调
- CSS 变量全部还原：--rose: #e8637a, --rose-light: #f2a0b0, --rose-dark: #c0475c, --rose-faint: #fdf0f2, --rose-soft: #fce8ec, --blush: #f7c5cd, --cream: #fffaf9
- 所有硬编码 rgba 值从蓝色(91,155,213) 改回粉色(232,99,122)
- Hero 渐变、Footer 径向渐变、导航栏背景、信封颜色全部还原
- JS 鼠标特效（点击文字+心形粒子）颜色数组改回粉色系
- **当前主题色：玫瑰粉 + 柔云白 + 深玫红**（与最初设计一致）

## 信封弹窗动画全面重构（参考图风格）
- 参考用户提供的信封素材图（image.png），完全重写 HTML/CSS/JS 三层结构
- **HTML**: 新增完整三角折叠结构（back-tri 左右内衬 + fold-left/right 正面折角 + fold-bottom 底部三角+红心 ♥ + letter-inside 内藏粉色信纸 + envelope-flap 顶部封盖）
- **CSS**: 信封尺寸从 360x260 增大到 420x320；使用 border 三角形拼接真实信封造型；封盖 3D 翻转 rotateX(180deg) 配弹性缓动；打开后粉色信纸向上消失；展开的信纸带左侧竖线装饰和顶部渐变条
- **JS**: 弹窗入场增加弹性缩放动画（scale(0.88)→1）；关闭时增加轻微上移缩小效果（translateY(-10px)+scale(0.92)）
- **动画时序**: 点击→封盖翻转 0.9s → 信纸缩回 0.7s → 展开信纸弹出 0.8s（延迟 0.35s）

## 粉色信封+立体红心风格重构
- 用户反馈之前的三角折叠信封样式不对，想要**第二种效果**：粉色/奶白色信封 + 中间**立体红色爱心**封口（类似参考图 image.png 的风格）
- **HTML 重构**: 移除了旧的内衬三角(back-tri)、正面折角(fold-left/right/fold-bottom)；新增 `env-front` 容器 + V形三角折边(`env-v-left`/`env-v-right`)；新增独立的红心封口组件(`envelope-seal` > `seal-heart` + `seal-shadow`)
- **CSS 全面重写**:
  - 信封主体改为粉白渐变背景(`#fff6f8 → #ffeef3 → #fce4ea → #f9d5df`)，圆角16px，更柔和的粉色调
  - 封盖颜色改为 `#fcd5e0`(淡粉)，带光泽层(`flap-shine`)增加质感
  - **立体红心** `seal-heart`: 字号3rem, 颜色#dc143c(深红), 多层text-shadow(外发光+内阴影)+filter drop-shadow营造3D立体感；底部有椭圆形阴影`seal-shadow`
  - 红心跳动动画 `sealHeartBeat`: 多阶段跳动(100%→112%→100%→108%→100%)，比普通心跳更有层次感
  - 打开时红心随封盖一起向上翻转消失(translateY(-60px) scale(0.7))
  - 信封尺寸进一步放大到 440×340
  - 背景模糊从8px增加到10px，遮罩透明度降低到0.5

## 白色信封+红心封口风格调整
- 用户再次反馈：不要粉色信封，要**白色信封✉️ + 中间红色爱心**（参考 image.png）
- **CSS 调整**:
  - 信封主体从粉白渐变改为**纯白/奶白渐变**（`#ffffff → #fafafa → #f5f5f5 → #f0f0f0`），阴影改为中性灰色
  - V形折边从粉色半透明改为**浅灰白色**（`rgba(235,236,240,0.85)`）
  - 顶部封盖从 `#fcd5e0`(淡粉) 改为**浅灰白** `#ececed`
  - 边缘高光线、光晕效果全部去除粉色调
  - 红色爱心封口保持不变（3D立体红心+心跳动画）

## 简洁白色信封+红心贴纸 完全重构（第三次）
- 用户再次反馈：不要任何复杂的折叠/三角/方框效果，就要**简洁的白色矩形信封 + 中间红色爱心贴纸**（参考 image.png）
- **HTML 完全重写**: 移除所有旧结构（envelope-body, envelope-flap, env-front, envelope-seal 等）；新结构为 `env-card > (env-letter + env-front-face > env-heart-sticker)` 三层嵌套
- **CSS 完全重写**:
  - 信封改为 `260×180px` 白色矩形卡片(`.env-card`)，`rotate(-2deg)` 微微倾斜，圆角12px
  - 内层 `.env-letter`: 8px内边距粉色渐变信纸（从边缘微微露出），带装饰线框
  - 正面 `.env-front-face`: 纯白渐变盖层，`transform-origin: center bottom`
  - 红心贴纸 `.env-heart-sticker`: 位于 `bottom:22%`，字号2.6rem，颜色#e63946，text-shadow+drop-shadow营造贴纸感+心跳动画
  - 打开时：env-card缩小到0.92消失；front-face rotateX(90deg)翻转；红心上移缩小
  - 弹窗尺寸缩小到 `400×300px`（更紧凑）
  - **去掉了所有三角border拼接、V形折边、封盖光泽、光晕层等复杂元素**

## 回退到第一版信封设计
- 用户反馈后续几版都不满意，明确要求**回退到对话记录中第一版的样式**
- 第一版特点：白色矩形信封(260×180px) + 顶部三角翻折封盖(`envelope-flap`) + 底部淡粉小心形♡ + 蓝色光晕呼吸效果(`envelope-glow`) + 内藏粉色信纸(`letter-inside`)
- **HTML**: 使用原始结构：`envelope-body > (letter-inside, env-front > back-tri left/right, envelope-flap, fold-bottom)`
- **CSS**:
  - 信封主体：`linear-gradient(#fff → #f7f8fa)`，圆角8px，蓝色调阴影
  - 封盖：`border-top-color: #e8ecf2`，`rotateX(180deg)`翻转动画
  - 光晕：`radial-gradient(rgba(100,160,220,0.12))` 蓝色呼吸
  - 弹窗尺寸：420×320px
  - 去掉了所有红心贴纸、V形线、深色边框等后期添加的元素

## 标准信封样式（最终版 - 无爱心）
- 用户要求：**去掉所有装饰元素，做成普通标准信封**
- **特点**: 干净白色矩形(260×180) + V形折痕SVG线 + 顶部翻折封盖 + 内层粉色信纸微微露出6px边缘 + 无爱心/无光晕
- **HTML**: `env-wrap > (letter-inside, env-front > svg polyline, env-flap)`
- **CSS**: 
  - 正面用 SVG `polyline` 绘制V形折痕(`stroke:#d4d8e0, opacity:0.45`)
  - 封盖 `border-top-color: #eceef3`，`rotateX(175deg)`翻转
  - 打开时正面也做 `rotateX(-100deg)` 翻转消失
  - 弹窗尺寸：400×300px

## 信封迭代（2026-05-09 下午）
- 在 env-front 中央加了小红心 `♥`（.env-heart），`top:48%, font-size:48px, color:#d94a5c, opacity:1`
- 信封整体放大：弹窗 560×440px，env-wrap 420×280px，封盖 border-left/right: 215px（去掉固定高度，改用 border-top-color）
- 动画性能优化：所有动画元素加 `will-change + backface-visibility:hidden`；`transition: all` 全部改为 `transform, opacity`
- 主站留言删除按钮已移除（仅 admin 后台可删除）
- admin.html 照片上传改造：支持文件选择/拖拽 → 自动上传 imgbb → 存入 D1 数据库；同时保留 URL 直接添加入口
